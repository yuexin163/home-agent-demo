# 产品库轻量检查报告

检查日期：2026-08-03

## 1. 原始文件

- 路径：`/Users/poro/Desktop/6.12壹品AI比赛材料/所需材料/产品库.xlsx`
- 文件大小：520,106,418 bytes（约 496 MiB）
- 系统识别格式：Microsoft OOXML
- ZIP 结构检查：正常，1,381 个内部条目；逐条 CRC 测试未发现损坏条目
- 原文件只读检查，未修改、移动、重命名、复制或解压到项目

## 2. 工作表

工作簿包含 12 个工作表：

1. 套系
2. 套系对比
3. 软件试用意见
4. 地板
5. 瓷砖
6. 厨房电器
7. 卫浴柜、洁具
8. 励致软装
9. 旺美居软装
10. 励致组合柜体
11. 建模记录
12. WpsReserved_CellImgList（veryHidden）

产品基础信息最可能位于“地板”“瓷砖”“厨房电器”“卫浴柜、洁具”“励致软装”“旺美居软装”和“励致组合柜体”。“套系/套系对比”适合提供套系映射，“建模记录”适合补充建模状态，不建议将 `WpsReserved_CellImgList` 作为业务表直接导入。

## 3. 嵌入媒体

- `xl/media` 文件数：1,324
- 媒体未压缩总体积：520,830,633 bytes（约 496.7 MiB）
- 媒体压缩总体积：519,484,326 bytes（约 495.4 MiB）
- 最大单个媒体：`xl/media/image411.png`，10,439,550 bytes（约 10.0 MiB）
- 第二大媒体：`xl/media/image412.png`，9,315,384 bytes（约 8.9 MiB）
- 至少 10 个媒体文件大于约 5 MiB

工作表 XML 总量很小：最大的 `sheet5.xml` 未压缩约 348 KiB，共享字符串未压缩约 129 KiB。由此可判断文件体积主要来自 1,324 个嵌入产品图片，而不是文本行数。

## 4. 少量样例提取

本阶段在不加载图片、不创建 DataFrame、不使用普通模式完整加载工作簿的前提下，只读取了 `xl/sharedStrings.xml` 与“地板”对应的 `xl/worksheets/sheet4.xml` 前 20 行。

已成功提取前 10 条产品文本样例到 `src/data/product-samples.json`。样例字段包括厂家、序号、商品名称、商品编码、类别、规格、面客价、建议位置、建模 ID 和上传状态。未提取或导出任何图片。表内价格保持原始数值，未推断币种或计价单位。

## 5. 后续建议

1. 将完整产品库清洗作为独立任务，继续以 ZIP/XML 流式读取为首选。
2. 按指定工作表分批解析文本，并建立明确的字段映射、单位字典与空值策略。
3. 先导出轻量 JSON/CSV 清单并进行去重、编码校验，再决定数据库结构。
4. 图片抽取应单独执行，按工作表关系和单元格图片 ID 建立映射，限制单批数量并做尺寸压缩。
5. 不要将原始 520 MB Excel 或全部图片放入前端仓库；正式图片应经过筛选、压缩并使用稳定命名。
6. 对“地板”表中的 `DISPIMG` 公式与 WPS 隐藏图片索引建立专门映射测试，避免文本记录和图片错配。

结论：文件格式正常，可安全进行定向、限量的文本和图片抽取；不适合直接完整加载或全量导入。当前 MVP 已获得 10 条地板文本样例，并在后续展示验证阶段选择 3 个品类、12款产品及12张对应图片。选择记录见 `docs/product-showcase-selection.md`。
